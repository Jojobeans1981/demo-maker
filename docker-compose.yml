services:
  demo-maker:
    build: .
    image: demo-maker:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  # Uncomment to add a database:
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: demo-maker
  #     POSTGRES_USER: app
  #     POSTGRES_PASSWORD: changeme
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

# volumes:
#   pgdata:
